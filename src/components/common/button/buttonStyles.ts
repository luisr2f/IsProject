import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    borderRadius: 4,
  },
  buttonContent: {
    paddingVertical: 0,
    paddingHorizontal: 4,
  },
  buttonLabel: {
    fontSize: 16,
    fontWeight: '500',
  },
});
